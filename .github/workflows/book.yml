# AUTO-GENERATED by scripts/gen_workflow.py — edit classes.yml then regenerate.
name: Book YMCA classes

on:
  schedule:
    - cron: "50 17 * * 1"  # vinyasa-yoga-mon — opens Mon 11:15 PT (PDT)
    - cron: "50 18 * * 1"  # vinyasa-yoga-mon — opens Mon 11:15 PT (PST)
    - cron: "55 18 * * 1"  # lift-hiit-mon — opens Mon 12:20 PT (PDT)
    - cron: "55 19 * * 1"  # lift-hiit-mon — opens Mon 12:20 PT (PST)
    - cron: "50 17 * * 2"  # cycle-tue — opens Tue 11:15 PT (PDT)
    - cron: "50 18 * * 2"  # cycle-tue — opens Tue 11:15 PT (PST)
    - cron: "5 17 * * 3"  # rpm-wed — opens Wed 10:30 PT (PDT)
    - cron: "5 18 * * 3"  # rpm-wed — opens Wed 10:30 PT (PST)
    - cron: "5 18 * * 3"  # core-wed — opens Wed 11:30 PT (PDT)
    - cron: "5 19 * * 3"  # core-wed — opens Wed 11:30 PT (PST)
    - cron: "50 17 * * 4"  # cycle-sculpt-thu — opens Thu 11:15 PT (PDT)
    - cron: "50 18 * * 4"  # cycle-sculpt-thu — opens Thu 11:15 PT (PST)
    - cron: "5 19 * * 4"  # therapeutic-yoga-thu — opens Thu 12:30 PT (PDT)
    - cron: "5 20 * * 4"  # therapeutic-yoga-thu — opens Thu 12:30 PT (PST)
    - cron: "20 17 * * 5"  # core-fri — opens Fri 10:45 PT (PDT)
    - cron: "20 18 * * 5"  # core-fri — opens Fri 10:45 PT (PST)
    - cron: "55 18 * * 5"  # lift-hiit-fri — opens Fri 12:20 PT (PDT)
    - cron: "55 19 * * 5"  # lift-hiit-fri — opens Fri 12:20 PT (PST)
  workflow_dispatch:
    inputs:
      class_key:
        description: "Class key to book now (see classes.yml). Blank = decide from schedule."
        required: false
        default: ""

concurrency:
  group: book-${{ github.run_id }}

jobs:
  book:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Write classes config
        env:
          CLASSES_YML: ${{ secrets.CLASSES_YML }}
        run: printf '%s' "$CLASSES_YML" > classes.yml
      - name: Book
        env:
          EGYM_USERNAME: ${{ secrets.EGYM_USERNAME }}
          EGYM_PASSWORD: ${{ secrets.EGYM_PASSWORD }}
        run: |
          if [ -n "${{ github.event.inputs.class_key }}" ]; then
            python -m src.main --class "${{ github.event.inputs.class_key }}"
          else
            python scripts/run_due.py
          fi
