# AUTO-GENERATED by scripts/gen_workflow.py — edit classes.yml then regenerate.
name: Book YMCA classes

on:
  schedule:
    - cron: "0 17 * * 1"  # core-mon — opens Mon 10:45 PT (PDT, primary, fire -45m)
    - cron: "0 18 * * 1"  # core-mon — opens Mon 10:45 PT (PST, primary, fire -45m)
    - cron: "15 17 * * 1"  # core-mon — opens Mon 10:45 PT (PDT, retry 1, fire -30m)
    - cron: "15 18 * * 1"  # core-mon — opens Mon 10:45 PT (PST, retry 1, fire -30m)
    - cron: "30 17 * * 1"  # core-mon — opens Mon 10:45 PT (PDT, retry 2, fire -15m)
    - cron: "30 18 * * 1"  # core-mon — opens Mon 10:45 PT (PST, retry 2, fire -15m)
    - cron: "30 17 * * 1"  # vinyasa-yoga-mon — opens Mon 11:15 PT (PDT, primary, fire -45m)
    - cron: "30 18 * * 1"  # vinyasa-yoga-mon — opens Mon 11:15 PT (PST, primary, fire -45m)
    - cron: "45 17 * * 1"  # vinyasa-yoga-mon — opens Mon 11:15 PT (PDT, retry 1, fire -30m)
    - cron: "45 18 * * 1"  # vinyasa-yoga-mon — opens Mon 11:15 PT (PST, retry 1, fire -30m)
    - cron: "0 18 * * 1"  # vinyasa-yoga-mon — opens Mon 11:15 PT (PDT, retry 2, fire -15m)
    - cron: "0 19 * * 1"  # vinyasa-yoga-mon — opens Mon 11:15 PT (PST, retry 2, fire -15m)
    - cron: "35 18 * * 1"  # lift-hiit-mon — opens Mon 12:20 PT (PDT, primary, fire -45m)
    - cron: "35 19 * * 1"  # lift-hiit-mon — opens Mon 12:20 PT (PST, primary, fire -45m)
    - cron: "50 18 * * 1"  # lift-hiit-mon — opens Mon 12:20 PT (PDT, retry 1, fire -30m)
    - cron: "50 19 * * 1"  # lift-hiit-mon — opens Mon 12:20 PT (PST, retry 1, fire -30m)
    - cron: "5 19 * * 1"  # lift-hiit-mon — opens Mon 12:20 PT (PDT, retry 2, fire -15m)
    - cron: "5 20 * * 1"  # lift-hiit-mon — opens Mon 12:20 PT (PST, retry 2, fire -15m)
    - cron: "10 16 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PDT, primary, fire -50m)
    - cron: "10 17 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PST, primary, fire -50m)
    - cron: "15 16 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PDT, retry 1, fire -45m)
    - cron: "15 17 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PST, retry 1, fire -45m)
    - cron: "30 16 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PDT, retry 2, fire -30m)
    - cron: "30 17 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PST, retry 2, fire -30m)
    - cron: "45 16 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PDT, retry 3, fire -15m)
    - cron: "45 17 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PST, retry 3, fire -15m)
    - cron: "50 16 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PDT, retry 4, fire -10m)
    - cron: "50 17 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PST, retry 4, fire -10m)
    - cron: "55 16 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PDT, retry 5, fire -5m)
    - cron: "55 17 * * 2"  # bodypump-tue — opens Tue 10:00 PT (PST, retry 5, fire -5m)
    - cron: "30 17 * * 2"  # cycle-tue — opens Tue 11:15 PT (PDT, primary, fire -45m)
    - cron: "30 18 * * 2"  # cycle-tue — opens Tue 11:15 PT (PST, primary, fire -45m)
    - cron: "45 17 * * 2"  # cycle-tue — opens Tue 11:15 PT (PDT, retry 1, fire -30m)
    - cron: "45 18 * * 2"  # cycle-tue — opens Tue 11:15 PT (PST, retry 1, fire -30m)
    - cron: "0 18 * * 2"  # cycle-tue — opens Tue 11:15 PT (PDT, retry 2, fire -15m)
    - cron: "0 19 * * 2"  # cycle-tue — opens Tue 11:15 PT (PST, retry 2, fire -15m)
    - cron: "45 9 * * 3"  # rpm-wed — opens Wed 10:30 PT (primary, fire -45m)
      timezone: "America/Los_Angeles"
    - cron: "0 10 * * 3"  # rpm-wed — opens Wed 10:30 PT (retry 1, fire -30m)
      timezone: "America/Los_Angeles"
    - cron: "15 10 * * 3"  # rpm-wed — opens Wed 10:30 PT (retry 2, fire -15m)
      timezone: "America/Los_Angeles"
    - cron: "45 10 * * 3"  # core-wed — opens Wed 11:30 PT (primary, fire -45m)
      timezone: "America/Los_Angeles"
    - cron: "0 11 * * 3"  # core-wed — opens Wed 11:30 PT (retry 1, fire -30m)
      timezone: "America/Los_Angeles"
    - cron: "15 11 * * 3"  # core-wed — opens Wed 11:30 PT (retry 2, fire -15m)
      timezone: "America/Los_Angeles"
    - cron: "10 16 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PDT, primary, fire -50m)
    - cron: "10 17 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PST, primary, fire -50m)
    - cron: "15 16 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PDT, retry 1, fire -45m)
    - cron: "15 17 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PST, retry 1, fire -45m)
    - cron: "30 16 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PDT, retry 2, fire -30m)
    - cron: "30 17 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PST, retry 2, fire -30m)
    - cron: "45 16 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PDT, retry 3, fire -15m)
    - cron: "45 17 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PST, retry 3, fire -15m)
    - cron: "50 16 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PDT, retry 4, fire -10m)
    - cron: "50 17 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PST, retry 4, fire -10m)
    - cron: "55 16 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PDT, retry 5, fire -5m)
    - cron: "55 17 * * 4"  # bodypump-thu — opens Thu 10:00 PT (PST, retry 5, fire -5m)
    - cron: "30 17 * * 4"  # cycle-sculpt-thu — opens Thu 11:15 PT (PDT, primary, fire -45m)
    - cron: "30 18 * * 4"  # cycle-sculpt-thu — opens Thu 11:15 PT (PST, primary, fire -45m)
    - cron: "45 17 * * 4"  # cycle-sculpt-thu — opens Thu 11:15 PT (PDT, retry 1, fire -30m)
    - cron: "45 18 * * 4"  # cycle-sculpt-thu — opens Thu 11:15 PT (PST, retry 1, fire -30m)
    - cron: "0 18 * * 4"  # cycle-sculpt-thu — opens Thu 11:15 PT (PDT, retry 2, fire -15m)
    - cron: "0 19 * * 4"  # cycle-sculpt-thu — opens Thu 11:15 PT (PST, retry 2, fire -15m)
    - cron: "0 17 * * 5"  # core-fri — opens Fri 10:45 PT (PDT, primary, fire -45m)
    - cron: "0 18 * * 5"  # core-fri — opens Fri 10:45 PT (PST, primary, fire -45m)
    - cron: "15 17 * * 5"  # core-fri — opens Fri 10:45 PT (PDT, retry 1, fire -30m)
    - cron: "15 18 * * 5"  # core-fri — opens Fri 10:45 PT (PST, retry 1, fire -30m)
    - cron: "30 17 * * 5"  # core-fri — opens Fri 10:45 PT (PDT, retry 2, fire -15m)
    - cron: "30 18 * * 5"  # core-fri — opens Fri 10:45 PT (PST, retry 2, fire -15m)
    - cron: "35 18 * * 5"  # lift-hiit-fri — opens Fri 12:20 PT (PDT, primary, fire -45m)
    - cron: "35 19 * * 5"  # lift-hiit-fri — opens Fri 12:20 PT (PST, primary, fire -45m)
    - cron: "50 18 * * 5"  # lift-hiit-fri — opens Fri 12:20 PT (PDT, retry 1, fire -30m)
    - cron: "50 19 * * 5"  # lift-hiit-fri — opens Fri 12:20 PT (PST, retry 1, fire -30m)
    - cron: "5 19 * * 5"  # lift-hiit-fri — opens Fri 12:20 PT (PDT, retry 2, fire -15m)
    - cron: "5 20 * * 5"  # lift-hiit-fri — opens Fri 12:20 PT (PST, retry 2, fire -15m)
  workflow_dispatch:
    inputs:
      class_key:
        description: "Class key to book now (see classes.yml). Blank = decide from schedule."
        required: false
        default: ""
      cancel_id:
        description: "Occurrence id to CANCEL an existing booking. Takes priority over class_key."
        required: false
        default: ""
      cancel_class:
        description: "Class key to CANCEL its next booked occurrence (optionally set cancel_on date)."
        required: false
        default: ""
      cancel_on:
        description: "With cancel_class: target class date YYYY-MM-DD (blank = next booked)."
        required: false
        default: ""
      cancel_paused:
        description: "Cancel ALL booked classes that now fall in a pause range (set to 'true')."
        required: false
        default: ""
      book_id:
        description: "Occurrence id to BOOK directly, independent of classes.yml (one-off exception)."
        required: false
        default: ""
      browse:
        description: "Books NOTHING: lists upcoming occurrences with full/joined so you can see what is still open. Use 'all' or a name filter, e.g. 'RPM'."
        required: false
        default: ""

concurrency:
  group: book-${{ github.run_id }}

jobs:
  book:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Book
        env:
          EGYM_USERNAME: ${{ secrets.EGYM_USERNAME }}
          EGYM_PASSWORD: ${{ secrets.EGYM_PASSWORD }}
          PRIVATE_REPO_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
          NOTIFY_EMAIL: ${{ secrets.NOTIFY_EMAIL }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          GITHUB_EVENT_SCHEDULE: ${{ github.event.schedule }}
        run: |
          if [ -n "${{ github.event.inputs.browse }}" ]; then
            # Read-only. Checked first so a stray value in another field can
            # never turn an availability check into a booking or cancellation.
            FILTER="${{ github.event.inputs.browse }}"
            [ "$FILTER" = "all" ] && FILTER=""
            python -m src.main --list "$FILTER"
          elif [ -n "${{ github.event.inputs.cancel_id }}" ]; then
            python -m src.main --cancel-id "${{ github.event.inputs.cancel_id }}"
          elif [ -n "${{ github.event.inputs.cancel_class }}" ]; then
            python -m src.main --cancel-class "${{ github.event.inputs.cancel_class }}" ${{ github.event.inputs.cancel_on && format('--on {0}', github.event.inputs.cancel_on) || '' }}
          elif [ -n "${{ github.event.inputs.cancel_paused }}" ]; then
            python -m src.main --cancel-paused
          elif [ -n "${{ github.event.inputs.book_id }}" ]; then
            python -m src.main --book-id "${{ github.event.inputs.book_id }}"
          elif [ -n "${{ github.event.inputs.class_key }}" ]; then
            python -m src.main --class "${{ github.event.inputs.class_key }}"
          else
            python scripts/run_due.py
          fi
