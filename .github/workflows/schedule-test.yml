# TEMPORARY — re-test of whether GitHub's scheduler fires for this repo, now
# that book.yml is back to a single file (revert in #120). Two one-shot fires
# today, ~15 and ~25 min out. Delete this file once we have an answer — see
# CLAUDE.md's "keep main clean" rule.
name: Schedule test (delete after use)

on:
  schedule:
    - cron: "17 23 31 8 *"  # 2026-08-31 23:17 UTC (4:17pm PDT) — one-shot
    - cron: "20 23 31 8 *"  # 2026-08-31 23:20 UTC (4:20pm PDT) — one-shot
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "Scheduled trigger fired at $(date -u) (event=${{ github.event_name }}, schedule=${{ github.event.schedule }})"
