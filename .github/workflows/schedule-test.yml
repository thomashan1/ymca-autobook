# TEMPORARY — re-test of whether GitHub's scheduler fires for this repo, now
# that book.yml is back to a single file (revert in #120). The previous two
# pairs today were either too-tight (inconclusive, <5 min registration
# window) or fired zero times with a proper buffer. This pair has a proper
# ~28/~43 min buffer, matching the methodology that already showed a clean
# failure yesterday. Delete this file once we have an answer — see CLAUDE.md's
# "keep main clean" rule.
name: Schedule test (delete after use)

on:
  schedule:
    - cron: "15 0 1 9 *"  # 2026-09-01 00:15 UTC (5:15pm PDT) — one-shot
    - cron: "30 0 1 9 *"  # 2026-09-01 00:30 UTC (5:30pm PDT) — one-shot
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "Scheduled trigger fired at $(date -u) (event=${{ github.event_name }}, schedule=${{ github.event.schedule }})"
